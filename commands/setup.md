---
description: Interview me and write brand/BRAND.md (run this first)
---
Use the `content-studio` skill.

If `./brand/BRAND.md` does not exist, create `./brand/` and `./output/` here and copy the
template from `${CLAUDE_PLUGIN_ROOT}/templates/brand/BRAND.md` and `${CLAUDE_PLUGIN_ROOT}/templates/brand/keywords.md`.

Then run `${CLAUDE_PLUGIN_ROOT}/agents/00-onboarding-agent.md` from the start. Ask one
question at a time and wait for each answer. If `./brand/BRAND.md` is already set up, ask
which field to change and update only that field. Finish by showing the file and listing
any required field still empty.
