---
description: Three content briefs for a topic (or "this week"), no script yet
argument-hint: <topic | this week>
---
Use the `content-studio` skill. Check `./brand/BRAND.md` is set up; if not, run
`/content-studio:setup` first. Then run `${CLAUDE_PLUGIN_ROOT}/agents/01-idea-agent.md`
for: **$ARGUMENTS**. Return three briefs from different pillars where possible, each with
three hooks that put a number, name or outcome in the first two words. Stop and let me
pick. Do not write a script.
