---
description: Find and download the logos, images and page captures a reel needs, with a licence ledger
argument-hint: <reel output folder>
---
Use the `content-studio` skill. Run `${CLAUDE_PLUGIN_ROOT}/agents/10-asset-agent.md` for
**$ARGUMENTS**.

Read that folder's `voiceover.md` and `resources.md`, list every brand, product, repo and
page the script names, and fetch what each one needs into `$ARGUMENTS/assets/` —
`logos/`, `images/`, `screens/` — writing a row into `assets/ASSETS.md` for every file
**as you go**.

Official source first. SVG preferred. Never modify a logo. Anything you cannot get
legitimately goes in the **Not obtained** table with the reason — never substitute a
lookalike.

Finish by telling me what is ✅ official, what is ⚠️ unverified, and what I need to
decide about.
