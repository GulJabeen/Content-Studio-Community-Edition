---
description: A slide-by-slide carousel plan with exact copy, for any design tool
argument-hint: <topic or reel folder>
---
Use the `content-studio` skill. Check `./brand/BRAND.md` is set up; if not, run
`/content-studio:setup` first. Run `${CLAUDE_PLUGIN_ROOT}/agents/07-carousel-agent.md`
for **$ARGUMENTS**, then `04-humanize` and `05-evaluator`. Write `carousel.md` and
`EVALUATION.md` to `./output/<YYYY-MM-DD>-<slug>/`.
